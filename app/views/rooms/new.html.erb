<div class="container mt-3 mb-3">
  <p><%= link_to request.referer.present? ? request.referer : root_path do %>
    <i class="fa-solid fa-backward"></i> Back
  <% end %></p>
  <div class="center-container text-center">
    <div class="container-content">

      <h1>Amygd's Crossing</h1>
      <%= render "shared/underline" %>
      <p class="mt-3">Select where you want to delve in first...</p>
    </div>
  </div>

  <div class="text-center">
    <div class="selection-form">
      <%= simple_form_for(@room) do |f| %>
      <%= f.input :journey_id,
                  as: :radio_buttons,
                  label: "",
                  collection_wrapper_tag: 'div',
                  collection_wrapper_class: 'category-wrapper',
                  item_wrapper_class: 'category-item',
                  input_html: { class: 'category-selector' },
                  # input_html: { class: "tag-selector" },
                  # item_wrapper_class: "tag-item",
                  collection: @journeys.each { |journey_id| journey_id },
                  class: "mt-2",
                  include_hidden: false %>
        <%= f.submit "Delve in", class: "btn btn-ocean mt-2" %>
      <% end %>
    </div>
  </div>
</div>
