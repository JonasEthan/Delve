<div class="container mt-3 mb-3">
  <p><%= link_to request.referer.present? ? request.referer : root_path do %>
    <i class="fa-solid fa-backward"></i> Back
  <% end %></p>

    <div class="center-container">
      <div class="container-content">
        <h1>Choose what <span class="special-font">Journeys</span> you want to embark on</h1>
        <%= render "shared/underline" %>
      <div>
        <p>I am going to give you all, a selection of mental disorders</p>
        <p class="mt-3">Choose these <span class="special-font">Journeys</span> wisely you never know what you could find or what could find you <span class="">....</span></p>
      </div>
      <div class="selection-form">
        <%= simple_form_for(@journey) do |f| %>
          <%= f.input :title,
                      label: "",
                      collection: @disorders.each { |disorder| disorder.name },
                      as: :check_boxes,
                      input_html: {class: "tag-selector" },
                      item_wrapper_class: "tag-item",
                      class: "mt-2",
                      include_hidden: false %>
          <%= f.submit "Embark!", class: "btn btn-ocean mt-2 special-font" %>
        <% end %>
      </div>
  </div>
</div>
